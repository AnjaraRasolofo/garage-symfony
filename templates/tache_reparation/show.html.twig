{% extends 'base.html.twig' %}

{% block title %}TacheReparation{% endblock %}

{% block body %}
    <h1>TacheReparation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tache_reparation.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ tache_reparation.description }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ tache_reparation.status }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ tache_reparation.dateDebut ? tache_reparation.dateDebut|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ tache_reparation.dateFin ? tache_reparation.dateFin|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tache_reparation_index') }}">back to list</a>

    <a href="{{ path('app_tache_reparation_edit', {'id': tache_reparation.id}) }}">edit</a>

    {{ include('tache_reparation/_delete_form.html.twig') }}
{% endblock %}
