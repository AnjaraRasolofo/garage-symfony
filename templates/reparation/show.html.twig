{% extends 'base.html.twig' %}

{% block title %}Reparation{% endblock %}

{% block body %}
    <h1>Reparation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reparation.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ reparation.description }}</td>
            </tr>
            <tr>
                <th>Date_reparation</th>
                <td>{{ reparation.dateReparation ? reparation.dateReparation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ reparation.status }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ reparation.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reparation_index') }}">back to list</a>

    <a href="{{ path('app_reparation_edit', {'id': reparation.id}) }}">edit</a>

    {{ include('reparation/_delete_form.html.twig') }}
{% endblock %}
